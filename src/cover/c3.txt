<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sample 22 - Glow Background + Reveal</title>
  <style>
    body { margin:0; height:100vh; background:#000; overflow:hidden; }
    .bg { position:absolute; inset:0; background:radial-gradient(circle at 20% 30%, #4285F460 0%, transparent 40%), radial-gradient(circle at 80% 70%, #EA433560 0%, transparent 50%), radial-gradient(circle at 50% 50%, #34A85340 0%, transparent 60%); filter:blur(80px); opacity:0.6; animation:pulse 20s infinite; }
    @keyframes pulse { 0%,100% { transform:scale(1); } 50% { transform:scale(1.15); } }
    .content { position:relative; z-index:1; height:100vh; display:grid; place-items:center; color:#fff; font-size:clamp(5rem,12vw,10rem); text-align:center; line-height:1; }
    .reveal { opacity:0; transform:translateY(80px); transition:all 1.8s cubic-bezier(0.23,1,0.32,1); }
    .reveal.show { opacity:1; transform:none; }
  </style>
</head>
<body>
  <div class="bg"></div>
  <div class="content">
    <div class="reveal">teacher name</div>
    <div class="reveal" style="font-size:0.8em; margin-top:20px;">with the next-generation IDE</div>
  </div>

  <script>
    document.querySelectorAll('.reveal').forEach((el,i) => {
      setTimeout(() => el.classList.add('show'), 800 + i*800);
    });
  </script>
</body>
</html>